---
Slot_Node:
  inheritance:
    extends: Slot
    type: concrete
  relations:
    Node:
      local: root_id
      foreignAlias: Slots
      onDelete: CASCADE

Slot_Theme:
  inheritance:
    extends: Slot
    type: concrete
  relations:
    Theme:
      local: root_id
      foreignAlias: Slots
      onDelete: CASCADE

Slot_Template:
  inheritance:
    extends: Slot
    type: concrete
  relations:
    Template:
      local: root_id
      foreignAlias: Slots
      onDelete: CASCADE